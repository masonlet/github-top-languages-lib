export const DEFAULT_CONFIG = {
  TITLE:     "Top Languages",
  WIDTH:     400,
  HEIGHT:    300,
  COUNT:     8,
  MIN_WIDTH: 400
} as const;

export const REFRESH_INTERVAL = 1000 * 60 * 60;
export const MAX_COUNT        = 16;
